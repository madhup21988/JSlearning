var marks=62;
//if marks grater than equal 60 then  allow for interview else dont allow

marks >=60 ? "then allow": "dont allow";


var num=4;
num%4==0 ? "even": "odd";

// If number is even then return "EVEN" else "ODD"
var num = 13;
var result = num%2 == 0 ? "EVEN" : "ODD";
console.log(`Given number is ${num} ==> ${result}`);